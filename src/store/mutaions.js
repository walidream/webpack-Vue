/**
 * count每次加1
 * @param {Object} state .
 */
export const incrementCount = (state) =>{
  state.count++;
};

/**
 * count每次减1
 * @param {Object} state .
 */
export const reductionCount = (state) =>{
  state.count--;
};
